package com.davidlu.interview;

import java.util.*;

public class ountCharacters {

    /*



    protected char[] countCharacter(String input)
    {
        char[] test =  new char[input.length()];
        int stringLength = input.length();


        //convert the string to the  character array
        for(int i=0;i<stringLength;i++ )
        {
            test[i]=input.charAt(i);
            //System.out.print(test[i]);
        }

        getDifferentElement(test, input);

        return test;
    }

    //filter and store the unique element in this array
    public void getDifferentElement(char[] test, String initial ) {

        Set<Character> uniqueString = new HashSet<Character>();
        for (int i = 0; i < test.length; i++) {
            uniqueString.add(test[i]);
        }

        // print the Set result
        System.out.println("This is the Set: " + uniqueString);

        //NEED to FIX here:
        Iterator<Character> i = uniqueString.iterator();
        char[] charArray = new char[uniqueString.size()];
        while (i.hasNext())
        {
            System.out.print(i);

        }

        //add into the newly created array
        for(int i=0;i<uniqueString.size();i++)
        {
            charArray[i] = uniqueString.

        }
    }
    public void countNumbers( String initialOne, char[] afterSet)
    {
        //add the key values into the new hashmap
        HashMap<Character, Integer> finalTest = new HashMap<Character, Integer>();
        for(int i=0;i<afterSet.length;i++)
        {
            finalTest.put(afterSet[i], null);
           // System.out.println(afterSet[i]);
        }
    }

    public static void main(String[] args) {
        // TODO Auto-generated method stub

        ountCharacters CountC = new ountCharacters();

        String a = "abffdfrrfrdrgrfddd";
        CountC.countCharacter(a);

    */
    }

















